// import React from 'react';
// import MuiLink from '@material-ui/core/Link';
// import { Link as GatsbyLink } from 'gatsby';
// import AniLink from "gatsby-plugin-transition-link/AniLink"

// const AniCustomLink = React.forwardRef(function Link(props, ref) {
//     return  <AniLink paintDrip to={to}><MuiCustomLink component={GatsbyLink} ref={ref} {...props} /></AniLink>;
//   });


// const MuiCustomLink = React.forwardRef(function Link(props, ref) {
//   return  <MuiLink component={GatsbyLink} ref={ref} {...props} />;
// });

// export default MuiCustomLink;