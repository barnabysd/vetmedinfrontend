import { useCookies, cookies, setCookie } from 'react-cookie'
import {tasksPoppy, 
    tasksDudley,
    tasksNewdog,
    tasksReggie} from '../WebsiteConstants'


export const saveCompletedTask = (task, dogChoice) => {
    //if (task.NNNNN)
    //setCookie(tasksDudley.DUDLEY_DETECT_HEART_MURMUR,true,"/")
}